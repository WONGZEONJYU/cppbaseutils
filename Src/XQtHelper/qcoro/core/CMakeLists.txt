# Coro 模块

aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR} SRC_FILES)

file(GLOB HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*.h*)
file(GLOB IMPL_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/impl/*.h*)
file(GLOB PRIVATE_HEADER_FILES ${CMAKE_CURRENT_SOURCE_DIR}/private/*.h*)
list(APPEND HEADER_FILES ${IMPL_HEADER_FILES} ${PRIVATE_HEADER_FILES})

add_xutils_library("${SRC_FILES}" "${HEADER_FILES}" "${CMAKE_CURRENT_SOURCE_DIR}")
